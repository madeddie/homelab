kube-prometheus-stack:
  alertmanager:
    ingress:
      enabled: true
      ingressClassName: traefik
      annotations:
        traefik.ingress.kubernetes.io/router.middlewares: ipallowlan
        traefik.ingress.kubernetes.io/router.entrypoints: web
      hosts:
        - host: alertmanager.svc.madtech.cx

