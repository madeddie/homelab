---
# Only allow localhost and LAN access
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ipallowlan
spec:
  ipAllowList:
    ipStrategy:
      depth: 1
    sourceRange:
      - 127.0.0.1/32
      - 192.168.0.0/24
